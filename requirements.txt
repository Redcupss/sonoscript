pyobjc-framework-Cocoa
pyobjc-framework-AVFoundation
pyobjc-framework-Quartz
certifi
py2app

# Kokoro (local neural voice provider)
kokoro-onnx
numpy
onnxruntime
espeakng-loader
phonemizer-fork
dlinfo
joblib
attrs

# Per-voice pitch shift (see pitch_shift.py)
psola
praat-parselmouth
pypar
tqdm
soundfile

# Chatterbox Turbo (MLX) — replaces Kokoro as the local neural voice provider (Phase 2)
mlx-audio

# Sesame license verification (see license.py) — Ed25519 via PyNaCl, offline, no network
pynacl

# Sesame voice-recording flow (see _startRecording in main.py) — already bundled transitively
# via mlx_audio, but main.py now imports it directly for microphone capture, so it's a real
# dependency of this app, not just an incidental one.
sounddevice
